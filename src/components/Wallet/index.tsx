import { AddrButton } from '@/components/Base/Button';

const Wallet = () => {
  return (
    <AddrButton>
      <span className="dot"></span>
      <span>000...addr</span>
    </AddrButton>
  );
};

export default Wallet;
